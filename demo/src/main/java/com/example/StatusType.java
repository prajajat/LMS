package com.example;

public class StatusType {
    public enum BorrowingStatus {
       PENDING,
        BORROWED,
        RETURNED
    }
    public enum GenderType{
        Male,
        Female,
        NotToSay
    }
}
